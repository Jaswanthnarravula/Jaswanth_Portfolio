# 02 — Hello page

## Role + requirement refs
The first thing a visitor sees at `/`: a premium liquid-glass stage with a beautifully morphed "Hello" — not a
portfolio hero. It ends with **Tap to begin**, the gesture that unlocks audio for the Netflix intro
(`03-netflix-page.md`). Requirements: R10, R40, R41, T. Kernel onboarding state: `hello`.

## Portfolio mapping
Only `person.name` and `person.headline` (server-rendered `<h1>` + subtitle). No other content — this page sets
tone, it does not list anything. A discreet "Résumé" link (`RES-PRE-01`) and "Skip the OS" link are present.

## Anatomy
```
┌──────────────────────────────────────────────────────────────┐
│ [Skip the OS]                                   Résumé · 🔊 │  top bar (text links + mute toggle)
│                                                              │
│                     ✍  Hello  (SVG morph)                    │  aria-hidden; static text alternative
│              Jaswanth — <headline>        ← <h1> LCP element │
│                                                              │
│                  ( Tap to begin  ◌ beam )                    │  glass pill button
│                                                              │
│  ambient light field (CSS gradient → WebGL on tier 2)        │
└──────────────────────────────────────────────────────────────┘
```
- **Hello glyph:** single-stroke handwriting paths for 5 greetings (Hello · Hola · Bonjour · नमस्ते · こんにちは),
  generated by `scripts/build-hello-paths.mjs` from an open-licensed single-line font, normalized + precompiled
  for MorphSVG. Any pair whose point-count mismatch exceeds the threshold crossfades instead of morphing.
- **Glass surfaces (≤ 3):** the Tap-to-begin pill, the top bar, and one large lens panel behind the glyph.
- **Tap to begin pill:** glass pill with a rotating conic border-beam (3 s linear, `transform`-only), as in the
  reference.

## Behaviour & states
| State | Behaviour |
|---|---|
| First paint (no JS yet) | `<h1>`, static drawn "Hello" (CSS `pathLength` stroke draw 1600 ms `cubic-bezier(0.65,0,0.35,1)`, `fill-mode: both`), pill visible as a real `<a href="#begin">`/button |
| Idle | After the draw, greetings morph every 2.4 s (morph 900 ms `power2.inOut`); loop pauses when the tab is hidden |
| Hover / focus on pill | Beam brightens; focus ring per `--focus-ring` |
| Press | Pill scales 0.97 (spring r 0.18 ζ 1); on release → `ONBOARDING_ADVANCE` → `intro` |
| Returning visitor (`prefs.introSeen`) | Hello and intro are skipped → lands on profiles |
| Reduced motion | Static drawn "Hello", no morph loop, no beam rotation; pill unchanged |
| `data-glass=solid` / tier 0 | Solid tinted surfaces, static gradient |

The Hello sequence **gates nothing**: the pill is interactive from first paint; tapping mid-draw advances immediately.

## Liquid glass by tier (`shared/06-design-system.md`, `shared/10-performance.md`)
- **T0:** tint over a pre-blurred static gradient; no `backdrop-filter`.
- **T1 (default):** `blur(24px) saturate(160%)`, specular edge pseudo-elements, 3 % noise; ±6 px pointer parallax
  on the background only. Optional Chromium-only SVG `feDisplacementMap` refraction on the pill (≤ 320 × 96) —
  cut if it costs frames.
- **T2:** `GlassStage` (imperative three.js, `lib/webgl`) — one fullscreen triangle, one fragment shader, zero
  render targets. DOM panels drop `backdrop-filter` and keep hairline + specular.

### Shader spec (`shaders/hello.frag`)
`field(uv)` = 4–5 drifting Gaussian lights in the brand palette (analytic, no fbm loops). Lens = SDF rounded
rectangles passed as `uniform vec4 uPanels[4]` (updated from a `ResizeObserver`, never per frame). Refraction:
`field(uv + n·k)` with `n` from the SDF gradient; dispersion = three evaluations (R/G/B offsets); fresnel edge
highlight; subtle film grain. Uniforms: `uTime`, `uPointer` (smoothed), `uAccent`, `uIntensity` (fades in 600 ms),
`uDim` (→ 1 when the Netflix layer takes over). DPR ≤ 1.5; 30 fps idle, full rate on input; paused when hidden.

## Navigation & routes
Route `/`, onboarding state `hello`. **No history writes** — all of `/` is one entry. Tap → `intro`.
"Résumé" → `/go/resume`. "Skip the OS" → `/plain`.

## Motion
Stroke draw (CSS, starts at first paint, no JS) · greeting morphs (GSAP MorphSVG, `welcome-motion` chunk, idle
loaded) · pill press spring · exit: glyph and panels fade 240 ms while the stage dims to `#141414` (`uDim`), handing
to the Netflix intro without a cut.

## Responsive
Phone portrait: single column, glyph sized in `svh`, pill ≥ 56 px tall, top bar collapses to icons with labels for
AT. Phone landscape: glyph left, pill right, no scroll. Tablet: centred 640 px column. Laptop/desktop: capped width;
T2 eligible. Safe-area tokens pad the top bar and pill.

## Accessibility
`<main>` with one `<h1>` ("Jaswanth — {headline}"). The morphing glyph is `aria-hidden`; a visually hidden
"Hello" precedes the `<h1>`. The pill is a real `<button>` named "Tap to begin". Mute toggle is a `<button
aria-pressed>` named "Sound". No autofocus (would scroll/zoom on mobile). First Tab → "Skip the OS".

## Edge cases
JS disabled → static page with links to `/plain`, `/go/resume` and each released OS. WebGL context lost → drop to T1
for the session. Morph chunk fails → static "Hello" stays. Very narrow (320 px) → glyph scales, nothing clips.
Tab hidden → loops pause. Double-tap on the pill → second tap is ignored (state already `intro`).

## Feature IDs + acceptance tests
| ID | Feature | Acceptance test |
|---|---|---|
| `HELLO-LCP-01` | SSR `<h1>` is the LCP element, never starts hidden | `lhci: LCP node is the h1; LCP ≤ 2.5 s (target 1.5 s)` |
| `HELLO-DRAW-01` | CSS stroke draw from first paint, no JS | `e2e: W2 no-js project shows drawn Hello` |
| `HELLO-MORPH-01` | Precompiled greeting morphs with crossfade fallback | `unit: every greeting pair has morph data or explicit crossfade` |
| `HELLO-PATHS-01` | Paths generated from an open-licensed single-line font | `unit: build-hello-paths output normalized + deterministic` |
| `HELLO-GLASS-01` | Glass by tier (T0/T1/T2), ≤ 3 blur surfaces | `e2e: X5 blur count ≤ 3; tier attributes switch styles` |
| `HELLO-GL-01` | `GlassStage` shader (tier 2 only, idle-loaded) | `perf: three chunk requested only after load; unit: dispose leaves memory zero` |
| `HELLO-TAP-01` | Tap to begin advances instantly at any time | `e2e: W1 tap during stroke draw advances to intro` |
| `HELLO-MUTE-01` | Mute toggle persists | `e2e: W3 muted preference survives reload` |
| `HELLO-RM-01` | Reduced-motion static state | `e2e: R1 no animation > 200 ms on /` |
| `HELLO-RETURN-01` | Returning visitors skip Hello + intro | `e2e: W1 second visit lands on profiles` |
| `HELLO-A11Y-01` | Semantics, names, tab order | `e2e: X1 axe clean; first Tab is Skip the OS` |
| `HELLO-RESP-01` | Layouts per size class | `e2e: X4 no horizontal scroll at 320 px; landscape has no scroll` |

## Not like the others
This is the only surface with WebGL and the only one whose look is Jaswanth's own brand (liquid glass). It must not
borrow any OS's chrome, and no OS may borrow its glass lens.
